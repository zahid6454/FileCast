fastapi==0.141.1
uvicorn[standard]==0.30.0
# Transitive via Authlib/pdfminer.six — pinned explicitly (CVE fix, PYSEC-2026-3552).
cryptography==50.0.0
httpx==0.27.0
python-multipart==0.0.32
pdf2docx==0.5.13
pdfplumber==0.11.4
openpyxl==3.1.5
PyMuPDF==1.24.11
python-pptx==1.0.2
opencv-python-headless==4.10.0.84
numpy==2.1.3
# Pinned to match the root requirements.txt's Pillow==12.* (build.py's OG-image
# dependency) — CI installs both files in one pip command, so a different pin
# here is a hard resolution conflict, not just a version mismatch. This file's
# own copy is what actually matters in production: the API's Docker image only
# installs api/requirements.txt, not the repo root's.
Pillow==12.*
sqlalchemy[asyncio]==2.0.*
psycopg[binary]==3.*
alembic==1.*
pydantic-settings==2.*
authlib==1.7.*
sentry-sdk[fastapi]==2.*
