# Dev/test-only dependencies (not shipped in the API image).
# The runtime deps (fastapi, httpx, python-multipart, sqlalchemy, psycopg,
# pydantic-settings) come from requirements.txt; install both to run the suite:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==8.*
pytest-asyncio==0.24.*
ruff==0.8.6
