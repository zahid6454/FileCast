# Static-site generator (build.py) dependencies.
jinja2
pyyaml
markdown
csscompressor
rjsmin

# Open Graph share image generation (P1 §7). ImageFont.load_default(size=...)
# (Pillow >= 10.1) ships an embedded scalable font inside the wheel itself —
# no system font, no network fetch, no separate rasterizer dependency needed.
Pillow==12.*

# Phase 2: build.py overlays the tool-state from Postgres via the shared api/data
# package (sync engine), the same deps seed.py needs. These are effectively
# optional to build.py — absent them the ImportError degrades to YAML-only (P10) —
# but installing them here activates the overlay on host builds (Phase 2 §6.2).
sqlalchemy[asyncio]==2.0.*
psycopg[binary]==3.*
pydantic-settings==2.*
# Phase 7.2: data.db now unconditionally imports data.node_registry (the N-way
# dynamic-engine active-node resolution), which imports data.redis_client —
# without this, the ImportError above is a ModuleNotFoundError on every single
# build, not a graceful degrade (confirmed: this exact failure appears in every
# deploy/release run's log, silently, since Phase 7.2 landed). Pinned to match
# api/requirements.txt exactly, same reasoning as this file's own Pillow pin
# above — a mismatch between the two is a hard resolver conflict elsewhere in
# CI where both files install together, not just a version difference.
redis==5.*
